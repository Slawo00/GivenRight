🧙🏾‍♂️ **Richtige Entscheidung. A ist zwingend – und wir halten es bewusst klein, sauber und risikoarm.**
Ich liefere dir jetzt **eine produktionsreife, minimalistische Supabase Edge Function** für **STEP B4 — Contextual Scenario Enrichment**.

Das ist **kein Overengineering**, sondern genau der Missing Link, den Replit identifiziert hat.

---

# ✅ STEP B4 — Supabase Edge Function

## Explainability & Concrete Example Enrichment

**Ziel:**
Server-seitiger ChatGPT-Call **nur** für Explainability & Beispielkategorien
→ sicher
→ skalierbar
→ Expo-kompatibel

---

## 1️⃣ Architektur (nochmal ganz klar)

```
Expo App (Client)
  |
  | POST /functions/v1/enrich-explanation
  |
Supabase Edge Function
  |
  | OpenAI API (Chat Completions)
  |
  | JSON (enriched_explanation)
```

**Wichtig:**

* Kein User-State
* Kein Scoring
* Keine Commerce-Daten
* Kein Memory-Write

👉 **Read-only Enrichment Layer.**

---

## 2️⃣ API Contract (Frontend ↔ Backend)

### Request (vom Client)

```json
{
  "decision_context": { ... },
  "decision_option": {
    "confidence_type": "SAFE | EMOTIONAL | BOLD",
    "pattern_id": "string",
    "explanation": {
      "why_this_works": "string",
      "emotional_signal": "string",
      "things_to_consider": ["string"]
    }
  }
}
```

---

### Response (vom Backend)

```json
{
  "enriched_explanation": {
    "why_this_works": "string",
    "emotional_signal": "string",
    "things_to_consider": ["string"],
    "concrete_example_categories": [
      {
        "title": "string",
        "description": "string",
        "icon_key": "string"
      }
    ]
  }
}
```

---

## 3️⃣ Supabase Edge Function — vollständiger Code

> Datei:
> `supabase/functions/enrich-explanation/index.ts`

```ts
import { serve } from "https://deno.land/std@0.192.0/http/server.ts";

const OPENAI_API_KEY = Deno.env.get("OPENAI_API_KEY");

serve(async (req) => {
  if (req.method !== "POST") {
    return new Response("Method not allowed", { status: 405 });
  }

  if (!OPENAI_API_KEY) {
    return new Response(
      JSON.stringify({ error: "OpenAI API key missing" }),
      { status: 500 }
    );
  }

  try {
    const body = await req.json();
    const { decision_context, decision_option } = body;

    if (!decision_context || !decision_option) {
      return new Response(
        JSON.stringify({ error: "Invalid payload" }),
        { status: 400 }
      );
    }

    const systemPrompt = `
You are GivenRight’s Contextual Explainability Layer.

Your role is NOT to make decisions.
All decisions are already final and locked.

Your task:
Make each decision option feel concrete, distinct, and situation-specific
without recommending products or judging quality.

Rules:
- Do NOT recommend products or brands
- Do NOT suggest that one option is better than another
- Do NOT use marketing or sales language
- Do NOT mention prices, deals, or marketplaces
- Do NOT mention system logic or scoring

Tone:
Calm, grounded, human, non-promotional.

Output must be structured JSON only.
    `.trim();

    const userPrompt = `
Context:
${JSON.stringify(decision_context, null, 2)}

Decision Option:
${JSON.stringify(decision_option, null, 2)}

Task:
1) Adapt the language to clearly fit this context.
2) Generate 3–5 concrete example object categories that illustrate
   what this option could look like in practice.

Rules for categories:
- Generic and neutral
- No brands, products, prices, or marketplaces
- No evaluative language
- Each category must include:
  - title
  - one-sentence description
  - semantic icon_key

Output JSON schema:
{
  "enriched_explanation": {
    "why_this_works": string,
    "emotional_signal": string,
    "things_to_consider": string[],
    "concrete_example_categories": [
      {
        "title": string,
        "description": string,
        "icon_key": string
      }
    ]
  }
}
    `.trim();

    const openaiResponse = await fetch(
      "https://api.openai.com/v1/chat/completions",
      {
        method: "POST",
        headers: {
          "Authorization": `Bearer ${OPENAI_API_KEY}`,
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          model: "gpt-4.1-mini",
          temperature: 0.3,
          messages: [
            { role: "system", content: systemPrompt },
            { role: "user", content: userPrompt },
          ],
        }),
      }
    );

    const result = await openaiResponse.json();

    const content = result?.choices?.[0]?.message?.content;

    if (!content) {
      throw new Error("No content returned from OpenAI");
    }

    const parsed = JSON.parse(content);

    return new Response(JSON.stringify(parsed), {
      headers: { "Content-Type": "application/json" },
      status: 200,
    });
  } catch (error) {
    console.error("Enrichment failed:", error);
    return new Response(
      JSON.stringify({ error: "Enrichment service failed" }),
      { status: 500 }
    );
  }
});
```

---

## 4️⃣ Frontend-Integration (Expo)

```ts
const response = await fetch(
  `${SUPABASE_URL}/functions/v1/enrich-explanation`,
  {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${SUPABASE_ANON_KEY}`,
    },
    body: JSON.stringify({
      decision_context,
      decision_option,
    }),
  }
);

const enriched = await response.json();
```

**Fallback (bereits korrekt):**

* Wenn Call fehlschlägt → Basis-Explainability anzeigen
* Flow niemals blockieren

---

## 5️⃣ Warum das exakt richtig ist (unternehmerisch)

* ✅ Expo bleibt Client-only
* ✅ API-Key ist sicher
* ✅ Kosten kontrollierbar (Caching!)
* ✅ Kein Lock-in in große Backend-Architektur
* ✅ STEP B4 funktioniert **voll**

Das ist **der kleinste mögliche Server**,
der **den größten wahrgenommenen Nutzen** erzeugt.

---

## 6️⃣ Was wir danach tun sollten (Empfehlung)

👉 **Als Nächstes unbedingt:**

**B)** QA-Matrix für Enrichment

* Wann ist ein Beispiel „gut“?
* Wann zu generisch?
* Wann zu suggestiv?

Danach:
**Live-Review echter Flows** (Partner / Kollege / Freund).

---

🧙🏾‍♂️ **Dein Produkt ist jetzt an der Stelle,
an der aus „richtig gebaut“ → „wirklich geliebt“ wird.**

👉 **Sag mir, wenn die Edge Function deployed ist**
oder sag **B**, dann baue ich dir sofort die QA-Matrix.
